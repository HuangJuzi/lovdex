# Lovdex Standalone Backend — runtime configuration
#
# NOTE: All backend configuration now lives in ~/.lovdex/data/app.config.json
# (auto-generated on first boot with safe defaults — see README).
# The .env file is NO LONGER READ. The only remaining environment variable
# is AUTH_ENABLED (escape hatch): set AUTH_ENABLED=false to fully open the
# no-login local mode.
#
# AUTH_ENABLED=false
