node_modules
node_modules/
dist/
dist-server/
dist-ssr/
*.log
.env
.env.*
!.env.example
# Root-level runtime data dirs only (anchored so it does not shadow app source
# directories literally named `data/`, e.g. web/src/components/*/data/).
/data/
*.db
*.sqlite
*.sqlite3
.DS_Store

# Supervisor runtime artifacts
supervisor/run.pid
supervisor/run.state.json
supervisor/logs/

# Superpowers brainstorm mockups
.superpowers/
